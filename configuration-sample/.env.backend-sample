# Node Configuration
NODE_ENV=production

# Super User Configuration
SUPER_USER_USERNAME=admin
SUPER_USER_PASSWORD=admin123
SUPER_USER_EMAIL=admin@example.com
SUPER_USER_FIRST_NAME=Super
SUPER_USER_LAST_NAME=Admin

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=86400

# MongoDB Configuration
MONGODB_URI=mongodb://mongouser:mongopass@mongo:27017/ewihub?authSource=admin

# SMTP Configuration
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password
SMTP_FROM="Image Generation" <noreply@example.com>

# Frontend URL (for password reset links)
FRONTEND_URL=http://localhost:3000

